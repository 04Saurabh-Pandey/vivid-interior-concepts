document.addEventListener('DOMContentLoaded',()=>{const box=document.createElement('div');box.className='lightbox';box.innerHTML='<button aria-label="Close image">&times;</button><img alt="Interior project image">';document.body.append(box);const img=box.querySelector('img'),close=()=>box.classList.remove('open');document.querySelectorAll('.works:not(#text)').forEach(card=>card.addEventListener('click',()=>{const bg=getComputedStyle(card).backgroundImage.match(/url\(["']?(.*?)["']?\)/);if(bg){img.src=bg[1];box.classList.add('open')}}));box.addEventListener('click',e=>{if(e.target===box||e.target.tagName==='BUTTON')close()});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})});
